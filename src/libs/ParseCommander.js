'use strict';

const {finished,pipeline,Transform,Readable} = require("stream");

class ParseCommander extends Transform {

  _transform(chunk, enc, next) {

  }
  _final(next) {
    
  }
}

module.exports = {ParseCommander}
